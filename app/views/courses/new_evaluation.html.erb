<%= form_for @course, url: 'evaluate', :html => { :multipart => true } do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this evaluation from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :Nota %><br>
    <%= f.number_field :difficulty, min:0, max:5  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
